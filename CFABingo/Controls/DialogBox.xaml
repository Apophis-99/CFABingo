<UserControl
    d:DesignHeight="500"
    d:DesignWidth="900"
    mc:Ignorable="d"
    x:Class="CFABingo.Controls.DialogBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CFABingo.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#CD000000">

        <Grid
            Height="150"
            Style="{StaticResource DialogBox}"
            Width="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" x:Name="DisplayText" />

            <Border
                BorderBrush="Black"
                BorderThickness="1"
                Grid.Row="1"
                Height="1" />

            <StackPanel
                Grid.Row="2"
                Orientation="Horizontal"
                x:Name="ButtonDisplay" />

        </Grid>

    </Grid>
</UserControl>
